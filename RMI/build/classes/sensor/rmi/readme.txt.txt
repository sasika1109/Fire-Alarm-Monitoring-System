Q.10

Singleton:

In server-side, it create only one instance and bind it to the RMI registry. Therefore, every client access the same object.
We can keep a single object in RMI registry and through that object we can access to the associate objects of the server side.